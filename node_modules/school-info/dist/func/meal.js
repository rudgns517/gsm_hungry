"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const axios_1 = __importDefault(require("axios"));
const meal = (object) => {
    const date = new Date();
    return axios_1.default.get('https://open.neis.go.kr/hub/mealServiceDietInfo', {
        params: Object.assign({
            Type: 'json',
            MLSV_YMD: date.getFullYear().toString() + (date.getMonth() + 1 < 10 ? '0' + (date.getMonth() + 1) : date.getMonth() + 1) + (date.getDate() < 10 ? '0' + date.getDate() : date.getDate())
        }, object)
    })
        .then(req => {
        return req.data.mealServiceDietInfo[1].row;
    })
        .catch(err => {
        return err;
    });
};
exports.default = meal;
